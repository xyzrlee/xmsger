dist: trusty

language: java

jdk:
- openjdk8

addons:
  sonarcloud:
    projectKey: "xyzrlee_api"
    organization: "xyzrlee-github"
    token:
      secure: "$SONAR_TOKEN"

script:
- cd xmsger
- chmod +x mvnw
- ./mvnw clean install -Dmaven.javadoc.skip=true -B -V
- ./mvnw clean org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar

cache:
  directories:
  - '$HOME/.m2/repository'
  - '$HOME/.sonar/cache'
